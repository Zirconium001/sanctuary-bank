spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sanctuary_bank
    username: postgres
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2}
    expiration-ms: 86400000
  bank:
    name: "Sanctuary of Equilibrium"
    code: "SOEB"
    swift: "SOEBINBB"
    rbi-license: "RBI-L-2024-SOEB-001"
